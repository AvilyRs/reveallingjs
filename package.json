{
  "name": "reveallingjs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "esbuild src/index.ts --bundle --outdir=src/build --watch",
    "build": "esbuild src/index.ts --bundle --minify --sourcemap --outdir=src/build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.19.7",
    "typescript": "^5.3.2"
  }
}
